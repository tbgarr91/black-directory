export function Footer() {
  return (
    <footer className="mt-auto border-t border-rule">
      <div className="mx-auto max-w-4xl px-6 py-8 text-sm text-ink-soft">
        <p>
          Every listing carries two independent marks: whether ownership has
          been verified, and whether the community rates it well. A business
          can be verified and still be a poor experience — we show both, so
          you can decide.
        </p>
      </div>
    </footer>
  );
}
